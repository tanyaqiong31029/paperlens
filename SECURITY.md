# 安全策略

## 支持版本

| 版本 | 支持状态 |
| --- | --- |
| 最新 main 分支 | ✅ 接受漏洞报告 |
| 历史提交 | ❌ 请先更新 |

## 如何报告漏洞

请使用 GitHub 的**私人漏洞报告**（仓库页 → Security → Report a vulnerability），**不要在公开 Issue 中描述未修复的安全问题**。我们会在 7 天内回应。

## 安全模型（先读这里）

PaperLens 设计为**完全本机自部署**，敏感数据有三类，请注意各自的边界：

1. **论文文本**：上传的文档、检测历史、自建语料全部存放在本机 `backend/data/`（SQLite + 文件），默认不入库、不出本机；
2. **API Key**（GPTZero 等外部引擎）：保存在本机 SQLite，通过管理界面配置，**绝不写入代码仓库**；调用外部 API 时仅发送待检测文本片段；
3. **管理员令牌**：`PAPERLENS_ADMIN_TOKEN` 用于写操作鉴权；默认服务只绑定 `127.0.0.1`。

## 有效漏洞范围（欢迎报告）

- Web 层：路径穿越、存储型/反射型 XSS（尤其 HTML 报告导出）、CSRF、管理员令牌鉴权绕过；
- 文件处理：恶意构造文档（docx/pdf）引发的解析崩溃或内存耗尽；
- 依赖漏洞：requirements / npm 依赖的已知 CVE 升级建议。

## 对部署者的建议

- **永远不要**把 `backend/data/`（含检测历史与 API Key）提交进仓库或打包分享；
- 向局域网/公网开放前**必须**设置强随机的 `PAPERLENS_ADMIN_TOKEN`（`openssl rand -hex 16`）；
- 提交 Issue 时**不要**粘贴 API Key、管理员令牌或论文原文——日志请先脱敏；
- 外部引擎 Key（如 GPTZero）请只在自己控制的账号下使用，并留意其用量账单。
